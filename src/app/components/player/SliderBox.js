
const SliderBox = () => {
    const sliderBoxClass = 'slider-box w-full h-auto relative'

    const sliderWrapperClass = 'slider-wrapper w-full h-auto flex flex-row gap-[6px]'

    return(
        <div
            className={sliderBoxClass}
        >

            <div
                className={sliderWrapperClass}
            >

                
                
            </div>

        </div>
    )
}

export default SliderBox