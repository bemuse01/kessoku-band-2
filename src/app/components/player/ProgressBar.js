import Slider from '@/components/public/Slider'

const ProgressBar = () => {
    const progressBarClass = 'progress-bar'

    return(
        <Slider className={progressBarClass} />
    )
}

export default ProgressBar